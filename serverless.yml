service: aws-s3cp-bulk
frameworkVersion: "=1.25.0"

provider:
  name: aws
  runtime: nodejs6.10
  region: ${opt:region, 'eu-west-1'}
  memorySize: 128
  timeout: 30

package:
  exclude:
    - tests/**
    - .circleci/**

functions:
  hello:
    handler: handler.hello
