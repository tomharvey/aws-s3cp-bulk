{
  "name": "aws-s3cp-bulk",
  "version": "1.0.0",
  "description": "Copy each file, in a list of files, from one bucket to another.",
  "main": "handler.js",
  "dependencies": {
    "aws-sdk": "^2.175.0",
    "url-parse": "^1.2.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.0.1"
  },
  "scripts": {
    "test": "find ./tests -name '*_test.js' | xargs mocha --timeout 15000 -R spec"
  },
  "author": "Tom Harvey",
  "license": "ISC",
  "repository": "github:tomharvey/aws-s3cp-bulk"
}
